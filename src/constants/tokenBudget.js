// Daily per-project token allotment for BrandSync Make.
// Single source of truth shared by the usage API (which reports it) and
// the UI meters (which fill against it). Counts input + output tokens.
export const DAILY_TOKEN_LIMIT = 150000;
