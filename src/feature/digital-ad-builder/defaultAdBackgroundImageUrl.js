/**
 * Default hero photo: bundled under /public (same-origin) so preview + export work without
 * expiring presigned URLs. Photo: office interior (Unsplash).
 */
export const DEFAULT_AD_BUILDER_BACKGROUND_IMAGE_URL = "/digital-ad-builder/default-ad-background.jpg";
