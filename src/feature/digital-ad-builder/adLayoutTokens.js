/**
 * Reference brand oranges (align with `valueTokens.json` Brand-Orange stops).
 */
export const REFERENCE_ORANGE = {
  orange: "#FF7500",
  o2Text: "#4D2300",
  cream: "#FFF1E5",
  white: "#FFFFFF",
};

/** Typography ratios from 1200px-wide reference artboard (headline 64px, body 36px, block gap 38px). */
export const PHOTO_BAND_TYPE = {
  headline: 64 / 1200,
  subtext: 36 / 1200,
  headlineSubGap: 38 / 1200,
  logoHeight: 85 / 1200,
  horizontalPad: 52 / 1200,
};
