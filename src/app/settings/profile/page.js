import ProfileContent from "@/feature/settings/profile/ProfileContent";

export const metadata = { title: "Profile – Settings" };

export default function ProfilePage() {
  return <ProfileContent />;
}
