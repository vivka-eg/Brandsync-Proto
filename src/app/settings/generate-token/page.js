import GenerateTokenContent from "@/feature/settings/generate-token/GenerateTokenContent";

export const metadata = { title: "MCP Token – Settings" };

export default function GenerateTokenPage() {
  return <GenerateTokenContent />;
}
