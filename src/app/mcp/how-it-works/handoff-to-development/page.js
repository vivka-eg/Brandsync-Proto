import HandoffToDevelopmentContent from "@/feature/mcp-how-it-works/HandoffToDevelopmentContent";

export const metadata = {
  title: "From Handoff to Development - BrandSync MCP",
  description:
    "Your flow is approved. BrandSync packages everything your developers need to build, and the pipeline keeps learning from what ships.",
};

export default function Page() {
  return <HandoffToDevelopmentContent />;
}
