import BrandsyncFoundationsContent from "@/feature/mcp-how-it-works/BrandsyncFoundationsContent";

export const metadata = {
  title: "BrandSync Foundations - BrandSync MCP",
  description:
    "Build your design foundation once, use it everywhere. Foundations generates production-ready theme code that brings EG's design tokens to life in your framework of choice.",
};

export default function Page() {
  return <BrandsyncFoundationsContent />;
}
